
// used for distance to cursor to stop at
var MAX_DIST = 200;

// used for different traversals
var RUNNING = 1;
var POUNCING = 2;

// used for spacing the div objects
// horizontal line to draw eyes at
var EYE_LINE = 50;
// the left most pad for the first eye
var EYE_LEFT_PAD = 23;

// the leftmost pad for the right eye
var EYE_RIGHT_PAD = EYE_LEFT_PAD + 37;

var HEAD_BODY_OFFSET = 0;
var HEAD_BOTTOM = 80;
